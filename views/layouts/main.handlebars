<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anime-Q</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header>
        <nav class="navbar navbar-dark bg-dark">
            <h1 class="title">Anime-Q</h1>
                 <a href="/api/anime/" class="btn" id="view-anime-btn">Animelist</a>
            {{#unless logged_in}}
                 <a href="/login" class="btn" id="nav-login-btn">Login</a>
                 <a href="/signup" class="btn" id="nav-signup-btn">Signup</a>
            {{/unless}}
            {{#if logged_in}}
                <a href="/watchlist" class="btn" id="nav-watchlist-btn">Watchlist</a>
                <button class="btn logout-btn" id="logout">Logout</button>
            {{/if}}
        </nav>
    </header>

    <main>
        {{{ body }}}
    </main>


    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>


</html>